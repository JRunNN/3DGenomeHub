import { GenericFilehandle } from 'generic-filehandle';
import BgzipIndexedFasta from '@gmod/indexedfasta/dist/bgzipIndexedFasta';
import IndexedFasta from '@gmod/indexedfasta/dist/indexedFasta';
declare function parseSmallFasta(text: string): {
    id: string;
    description: string;
    sequence: string;
}[];
declare class FetchableSmallFasta {
    fasta: GenericFilehandle;
    data: Promise<{
        id: string;
        description: string;
        sequence: string;
    }[]>;
    constructor({ fasta, path }: {
        fasta: GenericFilehandle;
        path: string;
    });
    fetch(id: string, start: number, end: number): Promise<string>;
    getSequenceNames(): Promise<string[]>;
}
export { parseSmallFasta, FetchableSmallFasta, IndexedFasta, BgzipIndexedFasta };
